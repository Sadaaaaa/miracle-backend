worker_processes 4;

events {
    worker_connections 1024;
}

http {

#     upstream app {
#         http://localhost:3000;
#     }

    server {
        listen 80;
        server_name 127.0.0.1;

        location / {
            proxy_pass http://host.docker.internal:3000;

        }
    }
}